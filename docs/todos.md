- migrate the latest structure changes on staging.

- run the scheduler on staging to test.

- create an assignment with a reminder notification.

- do a sql dump of the production database.

- is this going to capture the timezone of the person sending the reminder? or the user taking the assessment?

- go ahead and copy it into staging container so we can test without a deployment.